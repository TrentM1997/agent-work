## Tasks

- Refactor ConversationHandler to return structured results instead of writing to stdout
- Add multi-step agent loop with maxSteps
- Implement POST app/api/agent/run/route.ts
- Return final assistant message as JSON first
- Add tool call error handling and validation
- Add conversation history support
- Add streaming response path after non-streaming route works
